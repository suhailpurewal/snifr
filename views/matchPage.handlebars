<div id="matchContainer">
					{{#each Match}}
                	<div class="row" id="dogMatches">
                		<div class="col col-sm-2">
	                		<img src="{{this.dogPhoto}}" id="dogMatchImg">
	                	</div>
	                	<div class="col col-sm-6">
	                		<h2 id="dogMatchName"><a href="/profile/{{this.matchDog}}">{{this.dogName}} </a></h2>
	                		<hr>
	                		<h3 id="dogMatchBreed">Breed:{{this.dogBreed}} </h3>
	                	</div>
	                	<div class="col col-sm-4">
	                		<div class="row">
	                			<h2 id="dogMatchNumberHeader">Difference:{{this.scorediff}}</h2>
	                		</div>
	                		<div class="row">
	                			<h2 id="dogMatchNumber"></h2>
	                		</div>
	                	</div>

                	</div>
					{{/each}}
                </div>